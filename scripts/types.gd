const CHARACTER_CONTROLLER_PATH := 'res://scripts/character_controller.gd'
const HEALTH_PATH := 'res://scripts/health.gd'
const HIT_INF0_PATH := 'res://scripts/resources/hit_info.gd'
const Y_MAX_PATH := 'res://scripts/y_max.gd'

const CharacterController := preload(CHARACTER_CONTROLLER_PATH)
const Health := preload(HEALTH_PATH)
const HitInfo := preload(HIT_INF0_PATH)
const YMax := preload(Y_MAX_PATH)
